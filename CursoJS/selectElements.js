/*
    Selecionando elementos

    POR MARCA   -> getElementByTagName(aqui vai o nome da tag HTML)[entre chaves pode ser utilizadoo número da tag em ordem 0, 1, 2, 3. ]
    POR ID      -> getElementById(aqui vai o id dado pra tag HTML)
    POR NOME    -> getElementByName(aqui vai o nome dado pra tag HTML)
    POR CLASSE  -> getElementByClassName(mesmo propósito do Name só que agora é class)
    POR SELETOR -> querySelector(div.msg casso seja atribuido uma classe pra tag e #msg caso seja atrbuído um id)
                   querySelectorAll() <- plural do querySelector
*/